# Quick Task 260427-gi3: Fix chart grid resize and date-range x-axis zoom refresh bugs

**Date:** 2026-04-27
**Status:** Completed

## Goal

Fix two charting regressions:

- Switching a page from 1 column to 2 columns must resize charts and keep card action buttons inside each card.
- Changing the page date range must reset each chart's x-axis domain to the newly fetched data range as soon as the data arrives.

## Tasks

1. Update chart-card data reload handling so a query-context change clears stale X zoom/preview/cursor state before redrawing with the new series.
2. Tighten chart card/grid layout so two-column cards can shrink, headers wrap cleanly, and chart canvases/SVGs stay bounded to their grid cell.
3. Verify with static checks and, if practical, a local browser smoke test of grid toggle and date preset changes.
