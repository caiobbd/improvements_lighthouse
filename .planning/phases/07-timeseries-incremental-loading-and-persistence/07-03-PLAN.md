---
phase: 07-timeseries-incremental-loading-and-persistence
plan: 03
type: execute
wave: 2
depends_on:
  - 07-02
files_modified:
  - backend/app/api/charts.py
  - backend/app/services/profiling_adapter.py
autonomous: true
user_setup: []
must_haves:
  truths:
    - "Backend reuses recent timeseries responses for equivalent requests within TTL."
    - "Batch endpoint behavior remains compatible while reducing duplicated work."
  artifacts:
    - "backend/app/api/charts.py"
    - "backend/app/services/profiling_adapter.py"
  key_links: []
---

# Phase 7 Plan 03: Backend Timeseries Efficiency and Cache Strategy

## Objective

- **What:** Add backend-side timeseries caching and optimize batch execution paths.
- **Why:** Frontend improvements alone still overpay if backend repeats expensive adapter calls for identical payloads.
- **Output:** Efficient backend request handling with cache reuse and safer batch fanout behavior.

## Context

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-timeseries-incremental-loading-and-persistence/07-CONTEXT.md

## Tasks

<task type="code">
  <name>Introduce backend timeseries TTL cache</name>
  <files>backend/app/services/profiling_adapter.py</files>
  <action>Create cache keyed by item/attribute/date-window/frequency and return normalized series from cache when valid.</action>
  <verify>Repeated same-tag requests within TTL return cache hits and reduce adapter calls.</verify>
  <done>Backend avoids recomputing identical timeseries queries in short intervals.</done>
</task>

<task type="code">
  <name>Optimize /timeseries-batch execution path</name>
  <files>backend/app/api/charts.py</files>
  <action>Keep response contract stable while reducing duplicate per-tag work and enabling controlled parallelism if safe.</action>
  <verify>Batch response shape remains unchanged and equivalent payloads return faster under multi-tag load.</verify>
  <done>Batch endpoint is more efficient without breaking frontend contract.</done>
</task>

## Verification

- Compare latency for repeated identical batch requests before/after cache.
- Validate no regression in error handling per tag entry (`error` field still returned when needed).

## Success Criteria

- [ ] Backend serves repeated equivalent timeseries requests from cache within TTL.
- [ ] Batch endpoint maintains compatibility and improves repeated-load performance.
