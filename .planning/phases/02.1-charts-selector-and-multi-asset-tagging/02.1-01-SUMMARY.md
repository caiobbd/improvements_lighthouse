---
phase: 02.1-charts-selector-and-multi-asset-tagging
plan: 01
subsystem: charts-backend-selector
tags: [asset-search, timeseries-only, api-contract]
requires: []
provides:
  - Asset search endpoint with case-insensitive substring behavior
  - Search result cap enforcement (max 50)
  - Timeseries-only filtering in item attributes endpoint
affects: [02.1-02 selector UI, 02.1-04 verification]
tech-stack:
  added: []
  patterns: [backend-enforced-filtering, capped-search-contract]
key-files:
  modified:
    - backend/app/models/charts.py
    - backend/app/api/charts.py
    - backend/app/services/profiling_adapter.py
key-decisions:
  - "Expose selector-specific API surface through /asset-search."
  - "Enforce plot eligibility in backend with timeseries_only=true filter."
requirements-completed: [CHT-02, CHT-04]
duration: 35min
completed: 2026-04-13
---

# Phase 02.1-01 Summary

Implemented selector backend contracts for asset search and plot-eligible attribute filtering.

## Accomplishments
- Added `GET /api/v1/charts/asset-search` with case-insensitive substring matching and `limit <= 50`.
- Added `AssetSearchResponse` / `AssetSearchItem` response models.
- Extended `GET /api/v1/charts/item-attributes` with `timeseries_only` query parameter.
- Added adapter-level `search_assets(...)` helper with deterministic dedupe/sort/cap behavior.

## Verification
- `python -m py_compile backend/app/models/charts.py backend/app/api/charts.py backend/app/services/profiling_adapter.py` -> pass
- FastAPI `TestClient`:
  - `/api/v1/charts/asset-search?query=MV27&limit=50` -> `200`
  - `/api/v1/charts/item-attributes?...&timeseries_only=true` -> `200`, returned `non-timeseries-returned=0`

## Files
- `backend/app/models/charts.py`
- `backend/app/api/charts.py`
- `backend/app/services/profiling_adapter.py`
