---
phase: 02.1-charts-selector-and-multi-asset-tagging
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/models/charts.py
  - backend/app/api/charts.py
  - backend/app/services/profiling_adapter.py
autonomous: true
requirements: [CHT-02, CHT-04]
must_haves:
  truths:
    - "User can search assets via case-insensitive substring matching and receives at most 50 results."
    - "User can request item attributes filtered to timeseries-capable entries only."
  artifacts:
    - path: "backend/app/api/charts.py"
      provides: "Chart selector endpoints and filters for search/attributes"
      min_lines: 180
      contains: "asset-search"
    - path: "backend/app/models/charts.py"
      provides: "Typed response contracts for asset search and selector attributes"
      min_lines: 80
      contains: "ItemAttribute"
  key_links:
    - from: "backend/app/api/charts.py"
      to: "backend/app/services/profiling_adapter.py"
      via: "asset search and attribute filtering"
      pattern: "search|get_item_attributes"
---

<objective>
Create backend contracts required by selector flow: capped asset search + timeseries-only attribute retrieval.
</objective>

<execution_context>
@C:/Users/CaioBorges/_pgms/_repos/improvements_lighthouse/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/CaioBorges/_pgms/_repos/improvements_lighthouse/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02.1-charts-selector-and-multi-asset-tagging/02.1-CONTEXT.md
@.planning/phases/02.1-charts-selector-and-multi-asset-tagging/02.1-RESEARCH.md
@backend/app/api/charts.py
@backend/app/services/profiling_adapter.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add asset search endpoint contract</name>
  <files>backend/app/models/charts.py, backend/app/api/charts.py, backend/app/services/profiling_adapter.py</files>
  <action>Add a selector-focused asset search endpoint that performs case-insensitive substring matching and enforces a maximum 50-result cap.</action>
  <verify>python -m py_compile backend/app/models/charts.py backend/app/api/charts.py backend/app/services/profiling_adapter.py</verify>
  <done>Asset search endpoint supports selector UX contract with deterministic limits.</done>
</task>

<task type="auto">
  <name>Task 2: Add timeseries-only attribute filtering parameter</name>
  <files>backend/app/api/charts.py, backend/app/services/profiling_adapter.py</files>
  <action>Extend item attributes endpoint to support a `timeseries_only` flag so only attributes with real timeseries references are returned when requested.</action>
  <verify>python -m py_compile backend/app/api/charts.py backend/app/services/profiling_adapter.py</verify>
  <done>Selector can fetch only plot-eligible attributes from API.</done>
</task>

<task type="auto">
  <name>Task 3: Validate endpoint behavior with real assets</name>
  <files>backend/app/api/charts.py</files>
  <action>Run runtime checks for the two provided assets and confirm response contains IDs and timeseries-capable attributes.</action>
  <verify>findstr /C:"timeseries_only" backend\\app\\api\\charts.py</verify>
  <done>Backend selector endpoints are ready for frontend integration.</done>
</task>

</tasks>

<verification>
- [ ] Asset search is case-insensitive substring and capped at 50
- [ ] Attributes endpoint can return only timeseries-capable attributes
- [ ] Real API calls return valid payloads for target assets
</verification>

<output>
After completion, create `.planning/phases/02.1-charts-selector-and-multi-asset-tagging/02.1-01-SUMMARY.md`
</output>
