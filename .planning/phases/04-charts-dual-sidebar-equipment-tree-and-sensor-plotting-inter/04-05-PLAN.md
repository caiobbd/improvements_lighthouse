---
phase: 04-charts-dual-sidebar-equipment-tree-and-sensor-plotting-inter
plan: 05
type: execute
wave: 3
depends_on: [04]
files_modified:
  - frontend/charts/components/legend-panel.js
  - frontend/charts/components/chart-card.js
  - frontend/charts/styles/components.css
autonomous: true
requirements: [CHT-29]
must_haves:
  truths:
    - "Chart tag rendering uses a fixed-height scrollable table so long lists do not inflate card layout."
    - "Tag table keeps one information column (name) and preserves inline remove action per row."
    - "Remove action updates chart tags without full-page reload regressions."
  artifacts:
    - path: "frontend/charts/components/legend-panel.js"
      provides: "Tag table rendering with row toggle/remove controls"
      min_lines: 40
      contains: "legend"
    - path: "frontend/charts/components/chart-card.js"
      provides: "Chart tag remove wiring and tag-state update path"
      min_lines: 360
      contains: "selectedTags"
    - path: "frontend/charts/styles/components.css"
      provides: "Scrollable fixed-height legend table styles"
      min_lines: 520
      contains: "legend-panel"
  key_links:
    - from: "frontend/charts/components/chart-card.js"
      to: "frontend/charts/components/legend-panel.js"
      via: "chart card passes series + remove callback to table renderer"
      pattern: "createLegendPanel"
---

<objective>
Convert chart tag chips into a fixed-height, scrollable table with inline remove action while keeping existing tag toggle/remove behavior.
</objective>

<execution_context>
@C:/Users/CaioBorges/_pgms/_repos/improvements_lighthouse/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/CaioBorges/_pgms/_repos/improvements_lighthouse/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-charts-dual-sidebar-equipment-tree-and-sensor-plotting-inter/04-CONTEXT.md
@frontend/charts/components/legend-panel.js
@frontend/charts/components/chart-card.js
@frontend/charts/styles/components.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace legend chip renderer with table layout</name>
  <files>frontend/charts/components/legend-panel.js</files>
  <action>Render tags as table rows with one name column and one action column, preserving color marker and hide/show toggle behavior.</action>
  <verify>node --check frontend/charts/components/legend-panel.js</verify>
  <done>Tag list appears as table rows instead of chip wraps.</done>
</task>

<task type="auto">
  <name>Task 2: Add fixed-height scroll behavior and table visuals</name>
  <files>frontend/charts/styles/components.css</files>
  <action>Style legend panel as fixed-height scroll area with compact rows so long tag lists do not expand chart card height.</action>
  <verify>node --check frontend/charts/components/chart-card.js</verify>
  <done>Tag table stays inside fixed-height area and scrolls vertically when needed.</done>
</task>

<task type="auto">
  <name>Task 3: Keep inline row remove action wired to chart state updates</name>
  <files>frontend/charts/components/chart-card.js, frontend/charts/components/legend-panel.js</files>
  <action>Ensure remove button in each table row uses existing chart patch logic and does not trigger full-page rerender regressions.</action>
  <verify>node --check frontend/charts/components/chart-card.js && node --check frontend/charts/components/legend-panel.js</verify>
  <done>Removing a row tag updates only chart tag state and reloads chart data as expected.</done>
</task>

</tasks>

<verification>
- [ ] Tag area displays as single-name-column table with inline remove action
- [ ] Tag table has fixed-height scroll behavior for long lists
- [ ] Chart cards keep stable dimensions even with high tag counts
- [ ] Remove action updates chart tag state without full-page reload
</verification>

<output>
After completion, create `.planning/phases/04-charts-dual-sidebar-equipment-tree-and-sensor-plotting-inter/04-05-SUMMARY.md`
</output>
