---
phase: 04-charts-dual-sidebar-equipment-tree-and-sensor-plotting-inter
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/services/workspace_client.py
  - backend/app/services/profiling_adapter.py
  - backend/app/api/charts.py
  - backend/app/models/charts.py
autonomous: true
requirements: [CHT-20, CHT-21]
must_haves:
  truths:
    - "Backend exposes equipment-tree data for sidebar navigation."
    - "Backend exposes timeseries-only sensor attributes grouped by category for selected equipment."
  artifacts:
    - path: "backend/app/services/workspace_client.py"
      provides: "Workspace item tree retrieval and equipment-tree shaping"
      min_lines: 70
      contains: "Workspace"
    - path: "backend/app/api/charts.py"
      provides: "Charts API endpoints for equipment tree and categorized sensors"
      min_lines: 160
      contains: "item-attributes"
    - path: "backend/app/models/charts.py"
      provides: "Typed payload models for sidebar contracts"
      min_lines: 80
      contains: "BaseModel"
  key_links:
    - from: "backend/app/api/charts.py"
      to: "backend/app/services/workspace_client.py"
      via: "equipment tree endpoint uses workspace service"
      pattern: "get_item_tree"
---

<objective>
Add backend contracts required for equipment tree navigation and category-grouped timeseries sensor retrieval.
</objective>

<execution_context>
@C:/Users/CaioBorges/_pgms/_repos/improvements_lighthouse/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/CaioBorges/_pgms/_repos/improvements_lighthouse/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-charts-dual-sidebar-equipment-tree-and-sensor-plotting-inter/04-CONTEXT.md
@.planning/phases/04-charts-dual-sidebar-equipment-tree-and-sensor-plotting-inter/04-RESEARCH.md
@backend/app/services/workspace_client.py
@backend/app/services/profiling_adapter.py
@backend/app/api/charts.py
@backend/app/models/charts.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add equipment tree service contract from Workspace API</name>
  <files>backend/app/services/workspace_client.py, backend/app/services/profiling_adapter.py</files>
  <action>Wrap workspace item tree retrieval and normalize nodes for frontend tree rendering (id, name, parent_id, has_children/path hints).</action>
  <verify>python -m compileall backend/app</verify>
  <done>Backend can return stable equipment tree payload for sidebar 1.</done>
</task>

<task type="auto">
  <name>Task 2: Add API endpoint for equipment tree</name>
  <files>backend/app/api/charts.py, backend/app/models/charts.py</files>
  <action>Create `GET /api/v1/charts/equipment-tree` with typed response model and error handling for API connectivity failures.</action>
  <verify>python -m compileall backend/app</verify>
  <done>Frontend can query equipment tree from a dedicated endpoint.</done>
</task>

<task type="auto">
  <name>Task 3: Add categorized timeseries sensor endpoint for selected equipment</name>
  <files>backend/app/api/charts.py, backend/app/services/profiling_adapter.py, backend/app/models/charts.py</files>
  <action>Expose endpoint that reads item attributes, filters `data_source == Timeseries`, groups sensors by category, and keeps identity keys for checkbox and plotting logic.</action>
  <verify>python -m compileall backend/app</verify>
  <done>Sidebar 2 can request category-grouped timeseries sensors for one equipment.</done>
</task>

</tasks>

<verification>
- [ ] Equipment tree endpoint returns non-empty hierarchy for valid API connection
- [ ] Sensor endpoint returns timeseries-only entries grouped by category
- [ ] Payload models and compile checks pass without regression
</verification>

<output>
After completion, create `.planning/phases/04-charts-dual-sidebar-equipment-tree-and-sensor-plotting-inter/04-01-SUMMARY.md`
</output>
