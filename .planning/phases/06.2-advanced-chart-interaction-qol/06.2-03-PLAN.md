---
phase: 06.2-advanced-chart-interaction-qol
plan: 03
type: execute
wave: 3
depends_on:
  - 06.2-01
  - 06.2-02
files_modified:
  - frontend/charts/components/d3-line-chart.js
  - frontend/charts/components/chart-card.js
  - frontend/charts/components/legend-panel.js
  - frontend/charts/state/store.js
  - frontend/charts/styles/components.css
autonomous: true
user_setup: []
must_haves:
  truths:
    - "Y auto-scale can be applied without losing current X domain context."
    - "Normalization renders 0..1 scale for visible traces while tooltip shows original + normalized values."
    - "Split-Y mode is left-only, max 4 sensors, disabled with tooltip when unavailable, and auto-disables when threshold is exceeded."
  artifacts:
    - "frontend/charts/components/d3-line-chart.js"
    - "frontend/charts/components/chart-card.js"
    - "frontend/charts/state/store.js"
  key_links: []
---

# Phase 6.2 Plan 03: Axis Intelligence, Normalization, and Split-Y

## Objective

- **What:** Deliver axis-level controls for readability under mixed ranges and units.
- **Why:** Multi-sensor overlays become unreadable without adaptive scaling and per-trace axis support.
- **Output:** Y auto-scale, normalization mode, and guarded split-Y overlays with robust state transitions.

## Context

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.2-advanced-chart-interaction-qol/06.2-CONTEXT.md

## Tasks

<task type="code">
  <name>Add Y auto-scale control and behavior</name>
  <files>frontend/charts/components/d3-line-chart.js, frontend/charts/components/chart-card.js, frontend/charts/styles/components.css</files>
  <action>Add a Y auto-scale control near axis top and implement visible-data-fit recalculation for current chart domain context.</action>
  <verify>Auto-scale updates Y limits to visible data and keeps active X window unchanged.</verify>
  <done>Operators can restore variance visibility quickly after baseline shifts.</done>
</task>

<task type="code">
  <name>Add normalization mode with dual-value tooltip</name>
  <files>frontend/charts/components/d3-line-chart.js, frontend/charts/components/chart-card.js, frontend/charts/state/store.js</files>
  <action>Add chart action toggle for normalization (`0..1`) across visible traces and augment tooltip to show original and normalized values.</action>
  <verify>Normalization affects plotted scales only; tooltip still exposes true engineering value alongside normalized value.</verify>
  <done>Cross-unit traces become comparable without losing original-value context.</done>
</task>

<task type="code">
  <name>Add split-Y overlay with guardrails</name>
  <files>frontend/charts/components/d3-line-chart.js, frontend/charts/components/chart-card.js, frontend/charts/components/legend-panel.js, frontend/charts/state/store.js, frontend/charts/styles/components.css</files>
  <action>Implement split-Y mode (left-side colored labels), disable action with tooltip when >4 sensors, and auto-disable split mode with notice if sensor count exceeds threshold after updates; disable split mode while normalization is active.</action>
  <verify>Split-Y activation is restricted to <=4 sensors and guardrails trigger deterministic fallback when constraints are violated.</verify>
  <done>Multi-trace overlays remain readable and policy-compliant.</done>
</task>

## Verification

- `node --check frontend/charts/components/d3-line-chart.js`
- `node --check frontend/charts/components/chart-card.js`
- Manual browser check: normalization tooltip values, split-Y enable/disable rules, auto-disable notice path.

## Success Criteria

- [ ] Y auto-scale and normalization are available and stable.
- [ ] Tooltip shows original + normalized values in normalization mode.
- [ ] Split-Y guardrails enforce <=4 sensor policy with clear disabled-state feedback.
