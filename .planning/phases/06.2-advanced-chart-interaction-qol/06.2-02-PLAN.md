---
phase: 06.2-advanced-chart-interaction-qol
plan: 02
type: execute
wave: 2
depends_on:
  - 06.2-01
files_modified:
  - frontend/charts/components/d3-line-chart.js
  - frontend/charts/components/chart-card.js
  - frontend/charts/app.js
  - frontend/charts/styles/components.css
autonomous: true
user_setup: []
must_haves:
  truths:
    - "Marquee zoom supports free, X-locked (Ctrl), and Y-locked (Alt) selection."
    - "Shift+drag pans horizontally only and clamps to loaded range."
    - "Ctrl+Shift drag applies synced X zoom to all charts on active page with gray preview spans."
  artifacts:
    - "frontend/charts/components/d3-line-chart.js"
    - "frontend/charts/components/chart-card.js"
    - "frontend/charts/app.js"
  key_links: []
---

# Phase 6.2 Plan 02: Zoom, Pan, Sync, and Reset Workflows

## Objective

- **What:** Implement marquee/wheel zoom, shift-pan, synced X zoom previews, and reset controls.
- **Why:** Operators need fast macro-to-micro navigation across chart timelines while maintaining context between charts.
- **Output:** Full interaction workflows for zooming and panning, including cross-chart synchronization and explicit reset paths.

## Context

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.2-advanced-chart-interaction-qol/06.2-CONTEXT.md

## Tasks

<task type="code">
  <name>Add marquee zoom and wheel zoom behaviors</name>
  <files>frontend/charts/components/d3-line-chart.js</files>
  <action>Implement drag selection rectangle (free/X-only/Y-only by modifiers) plus cursor-anchored wheel zoom (`Ctrl`: X, `Alt`: Y).</action>
  <verify>Selection and wheel interactions update domains correctly and keep cursor-focused zoom intent.</verify>
  <done>Chart supports robust zoom flows without interaction ambiguity.</done>
</task>

<task type="code">
  <name>Add shift-pan with loaded-range clamping</name>
  <files>frontend/charts/components/d3-line-chart.js</files>
  <action>Implement `Shift+drag` horizontal pan and clamp movement to available loaded data bounds.</action>
  <verify>Panning cannot move beyond loaded series range; Y remains fixed during pan.</verify>
  <done>Time scrubbing behavior is smooth and bounded.</done>
</task>

<task type="code">
  <name>Add synced X zoom preview/apply + reset controls</name>
  <files>frontend/charts/components/d3-line-chart.js, frontend/charts/components/chart-card.js, frontend/charts/app.js, frontend/charts/styles/components.css</files>
  <action>Implement `Ctrl+Shift+drag` synced X selection with gray preview span on every active-page chart; add double-click reset and `Reset View` chart action.</action>
  <verify>All charts on active page receive synced X-domain update; reset works from both double-click and action menu.</verify>
  <done>Cross-chart navigation and recovery flows are fully operational.</done>
</task>

## Verification

- `node --check frontend/charts/components/d3-line-chart.js`
- `node --check frontend/charts/components/chart-card.js`
- Manual browser check: synced zoom preview and apply across all charts, plus double-click/action reset.

## Success Criteria

- [ ] Marquee and wheel zoom interactions match defined modifier contract.
- [ ] Shift-pan is horizontal-only and clamped to loaded range.
- [ ] Synced X zoom and reset controls work reliably on active page charts.
