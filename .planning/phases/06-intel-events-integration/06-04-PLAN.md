---
phase: 06-intel-events-integration
plan: 04
type: execute
wave: 4
depends_on:
  - 06-03
files_modified:
  - backend/app/services/profiling_adapter.py
  - backend/app/api/charts.py
  - frontend/charts/app.js
  - frontend/charts/components/d3-line-chart.js
autonomous: true
user_setup: []
must_haves:
  truths:
    - "Alarm pages create one chart per matched event timeseries attribute."
    - "Attribute resolution tries exact-name matching first."
    - "All alarm charts show a span from alarm start_date to end_date (or now)."
  artifacts:
    - "frontend/charts/components/d3-line-chart.js"
    - "frontend/charts/app.js"
  key_links: []
---

# Phase 6 Plan 04: Alarm Attribute Plotting and Span Overlays

## Objective

- **What:** Resolve event-provided attribute names into plottable tags and render alarm window spans across all charts in alarm pages.
- **Why:** Alarm analysis requires both the relevant sensors and visual event window context.
- **Output:** Alarm charts generated from event attributes with consistent event span overlays.

## Context

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-intel-events-integration/06-CONTEXT.md
@frontend/charts/components/d3-line-chart.js

## Tasks

<task type="code">
  <name>Resolve event timeseries attributes to chart tags</name>
  <files>backend/app/services/profiling_adapter.py, backend/app/api/charts.py, frontend/charts/app.js</files>
  <action>Implement matching pipeline for `data.event_timeseries_attributes` against selected event item attributes, prioritizing exact-name matches first and emitting unresolved-name warnings.</action>
  <verify>Alarm page receives one chart tag per matched attribute name and reports unresolved attributes in warning channel/header.</verify>
  <done>Event attributes reliably map to chartable tags.</done>
</task>

<task type="code">
  <name>Generate one chart per matched attribute on alarm pages</name>
  <files>frontend/charts/app.js, frontend/charts/state/store.js</files>
  <action>Create chart definitions from matched attributes for alarm page initialization, preserving alarm window date filters and page metadata.</action>
  <verify>Alarm page creates N charts for N matched attributes with no duplicate insertion on initial generation.</verify>
  <done>Alarm page content is ready for immediate investigation.</done>
</task>

<task type="code">
  <name>Render alarm window span in every alarm chart</name>
  <files>frontend/charts/components/d3-line-chart.js, frontend/charts/app.js</files>
  <action>Add chart overlay rendering for alarm span (`start_date` to `end_date|now`) and ensure span metadata is provided to all alarm-page charts.</action>
  <verify>Each alarm chart displays the same event window span and remains compatible with existing chart interactions.</verify>
  <done>Operators can visually correlate trends with alarm duration.</done>
</task>

## Verification

- `python -m compileall backend/app`
- Run node syntax check for frontend/charts/app.js
- Manual: alarm page chart set matches resolved attributes and each chart shows alarm span.

## Success Criteria

- [ ] Alarm pages create one chart per matched event attribute.
- [ ] Attribute matching applies exact-name-first logic and warnings for misses.
- [ ] Every alarm chart shows a consistent alarm span overlay.
