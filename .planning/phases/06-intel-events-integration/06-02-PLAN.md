---
phase: 06-intel-events-integration
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - frontend/charts/services/api-client.js
  - frontend/charts/app.js
  - frontend/charts/styles/components.css
autonomous: true
user_setup: []
must_haves:
  truths:
    - "Sidebar 2 defaults to Sensors and supports switching to Events."
    - "Events tab supports status-based filtering with all available status values."
    - "Events are displayed as cards with required fields."
  artifacts:
    - "frontend/charts/app.js"
    - "frontend/charts/services/api-client.js"
  key_links: []
---

# Phase 6 Plan 02: Sidebar Events Tab UX

## Objective

- **What:** Add an `Events` tab in sidebar 2 and render equipment-filtered event cards with status filtering.
- **Why:** Operators need to browse alarms in the same navigation workflow used for sensors.
- **Output:** Sidebar 2 tabbed interface: `Sensors | Events`, with event cards and context actions.

## Context

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-intel-events-integration/06-CONTEXT.md
@frontend/charts/app.js

## Tasks

<task type="code">
  <name>Extend API client for equipment events retrieval</name>
  <files>frontend/charts/services/api-client.js</files>
  <action>Add client calls for new equipment events endpoint, including selected item, subtree scope, and status filtering params.</action>
  <verify>Client function returns expected payload shape with cards and status options.</verify>
  <done>Frontend can fetch event data with a single API function.</done>
</task>

<task type="code">
  <name>Add Sidebar-2 tab switch and events state handling</name>
  <files>frontend/charts/app.js</files>
  <action>Implement tab state for `Sensors` and `Events` (default Sensors), event loading on equipment selection, and status filter controls.</action>
  <verify>Switching tabs preserves equipment selection and renders the correct panel content.</verify>
  <done>User can browse either sensors or events from the same equipment context.</done>
</task>

<task type="code">
  <name>Render event cards and card context menu action</name>
  <files>frontend/charts/app.js, frontend/charts/styles/components.css</files>
  <action>Create event card UI with required fields and right-click menu containing `Plot alarm in new page`.</action>
  <verify>Event cards appear sorted newest-first and context action is visible/triggerable.</verify>
  <done>Events tab behavior matches planned interaction model.</done>
</task>

## Verification

- Run node syntax check for frontend/charts/app.js
- Manual sidebar check: Sensors default, Events tab available, status filter options populated.

## Success Criteria

- [ ] Sidebar 2 defaults to Sensors and offers Events tab.
- [ ] Events render as newest-first cards with required fields.
- [ ] Status filtering and right-click action are operational.
