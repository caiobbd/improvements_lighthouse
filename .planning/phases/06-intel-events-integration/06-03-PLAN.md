---
phase: 06-intel-events-integration
plan: 03
type: execute
wave: 3
depends_on:
  - 06-01
  - 06-02
files_modified:
  - frontend/charts/state/store.js
  - frontend/charts/app.js
  - frontend/charts/components/page-controls.js
  - frontend/charts/styles/components.css
autonomous: true
user_setup: []
must_haves:
  truths:
    - "Plot alarm action always creates a new alarm page named '[Alarm] {name}'."
    - "Alarm page date window follows fallback policy: trend_start -> end_date-300d -> now-300d."
    - "Alarm header row shows alarm details and missing-data warnings."
  artifacts:
    - "frontend/charts/state/store.js"
    - "frontend/charts/app.js"
  key_links: []
---

# Phase 6 Plan 03: Alarm Page Generation and Header Row

## Objective

- **What:** Convert event-card action into a full alarm-page creation flow with alarm metadata row and controlled date window defaults.
- **Why:** Alarm investigations need dedicated pages with clear event context and reproducible plotting windows.
- **Output:** New `[Alarm] {name}` pages with alarm context row and deterministic time windowing.

## Context

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-intel-events-integration/06-CONTEXT.md
@frontend/charts/state/store.js

## Tasks

<task type="code">
  <name>Implement alarm page factory from event cards</name>
  <files>frontend/charts/app.js, frontend/charts/state/store.js</files>
  <action>Wire `Plot alarm in new page` to always create a new page named `[Alarm] {name}` with alarm metadata attached to page state.</action>
  <verify>Repeated action for same event creates distinct pages rather than reusing existing page.</verify>
  <done>Event-to-page workflow is deterministic and user-controlled.</done>
</task>

<task type="code">
  <name>Apply alarm time window defaults and fallbacks</name>
  <files>frontend/charts/app.js, frontend/charts/state/store.js</files>
  <action>Set alarm page date range from `trend_display_start_date`; fallback to `end_date - 300 days`, then `now - 300 days`; end uses `end_date` or `now`.</action>
  <verify>Alarm pages always initialize with valid start/end datetimes even when event fields are missing.</verify>
  <done>Window policy is fully encoded and resilient to incomplete payloads.</done>
</task>

<task type="code">
  <name>Add alarm header row and missing-data warnings</name>
  <files>frontend/charts/app.js, frontend/charts/components/page-controls.js, frontend/charts/styles/components.css</files>
  <action>Render alarm summary row above chart grid with card fields plus description/prognosis/status and explicit warning text for missing values.</action>
  <verify>Alarm pages display alarm summary row and warnings; non-alarm pages keep existing layout.</verify>
  <done>Alarm context is visible before chart inspection.</done>
</task>

## Verification

- Run node syntax check for frontend/charts/app.js
- Manual: create alarm page from event card and confirm header row + fallback date range behavior.

## Success Criteria

- [ ] Alarm page creation always generates new `[Alarm] {name}` pages.
- [ ] Alarm page date-window fallback chain is correctly applied.
- [ ] Alarm metadata row and missing-field warnings render correctly.
