---
phase: 06.1-alarm-row-parity-with-lighthouse-full-alarm-window
plan: 02
type: execute
wave: 2
depends_on:
  - 06.1-01
files_modified:
  - frontend/charts/app.js
  - frontend/charts/styles/components.css
autonomous: true
user_setup: []
must_haves:
  truths:
    - "Alarm row hierarchy matches Lighthouse intent: title + metadata chips + focused details + narrative zone."
    - "Alarm summary remains readable at current desktop widths without breaking chart controls."
    - "Non-alarm pages are unaffected by alarm-row layout changes."
  artifacts:
    - "frontend/charts/app.js"
    - "frontend/charts/styles/components.css"
  key_links: []
---

# Phase 6.1 Plan 02: Lighthouse-like Alarm Row Shell and Layout

## Objective

- **What:** Rebuild the alarm information row structure and styling for parity with Lighthouse full alarm window hierarchy.
- **Why:** Current alarm row is functional but visually flat; operators need faster scanning of critical metadata.
- **Output:** New alarm row shell with stronger visual hierarchy and responsive layout behavior.

## Context

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06.1-alarm-row-parity-with-lighthouse-full-alarm-window/06.1-CONTEXT.md
@frontend/charts/styles/components.css

## Tasks

<task type="code">
  <name>Refactor alarm-row DOM structure for hierarchy parity</name>
  <files>frontend/charts/app.js</files>
  <action>Update `renderAlarmDetailsRow` markup into structured zones: title/header row, metadata chips row, key facts row, narrative container, warnings block.</action>
  <verify>Alarm row order and grouping follow the intended Lighthouse-like scan flow.</verify>
  <done>Alarm pages render a clearly segmented info row before charts.</done>
</task>

<task type="code">
  <name>Implement alarm-row visual hierarchy styling</name>
  <files>frontend/charts/styles/components.css</files>
  <action>Add/adjust CSS classes for typography contrast, compact chips, spacing, and emphasis for status/start/elapsed while preserving dark theme language.</action>
  <verify>Visual inspection confirms stronger hierarchy and no style bleed into non-alarm pages.</verify>
  <done>Alarm row communicates key facts at a glance and remains consistent with the Charts page style system.</done>
</task>

<task type="code">
  <name>Harden responsive behavior for alarm row</name>
  <files>frontend/charts/styles/components.css, frontend/charts/app.js</files>
  <action>Ensure wrap/truncation rules for long asset names and ids, with stable row spacing at common desktop breakpoints.</action>
  <verify>Alarm row does not overflow or overlap controls/charts at widths used during verification.</verify>
  <done>Readable, stable alarm row across current target viewport sizes.</done>
</task>

## Verification

- `node --check frontend/charts/app.js`
- Manual UI check on alarm page: verify hierarchy parity, chip readability, and non-alarm pages unaffected.

## Success Criteria

- [ ] Alarm row hierarchy visually matches Lighthouse intent.
- [ ] Layout remains readable/responsive at current desktop widths.
- [ ] Non-alarm page layout behavior remains unchanged.
