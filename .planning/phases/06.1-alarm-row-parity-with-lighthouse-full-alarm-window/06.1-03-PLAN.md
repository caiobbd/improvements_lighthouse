---
phase: 06.1-alarm-row-parity-with-lighthouse-full-alarm-window
plan: 03
type: execute
wave: 3
depends_on:
  - 06.1-01
  - 06.1-02
files_modified:
  - frontend/charts/app.js
  - frontend/charts/styles/components.css
  - frontend/charts/utils/safe-markdown.js
autonomous: true
user_setup: []
must_haves:
  truths:
    - "Prognosis and description are rendered from payload markdown without fixed section parsing."
    - "Rendered markdown output is sanitized and does not allow unsafe HTML/script injection."
    - "If markdown rendering fails, raw text fallback is available in the alarm row."
  artifacts:
    - "frontend/charts/utils/safe-markdown.js"
    - "frontend/charts/app.js"
  key_links: []
---

# Phase 6.1 Plan 03: Sanitized Markdown Narrative Rendering

## Objective

- **What:** Add sanitized markdown rendering for alarm narrative fields and fallback behavior.
- **Why:** Alarm payload narratives are variable and long; rendering markdown improves readability while preserving safety.
- **Output:** Safe markdown pipeline for `description` and `prognosis` with raw-text fallback.

## Context

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06.1-alarm-row-parity-with-lighthouse-full-alarm-window/06.1-CONTEXT.md
@frontend/charts/app.js

## Tasks

<task type="code">
  <name>Create safe markdown utility</name>
  <files>frontend/charts/utils/safe-markdown.js</files>
  <action>Implement lightweight markdown-to-HTML conversion for key syntax (paragraphs, bold, lists, line breaks) with strict sanitization/escaping.</action>
  <verify>Unsafe HTML/script payload fragments are escaped and not executed in rendered output.</verify>
  <done>Reusable utility exists for alarm narrative rendering without external dependency risk.</done>
</task>

<task type="code">
  <name>Integrate markdown renderer in alarm row</name>
  <files>frontend/charts/app.js</files>
  <action>Replace plain-text narrative rendering with sanitized markdown output for `description` and `prognosis` from normalized alarm model.</action>
  <verify>Known markdown payloads (bullets, emphasis, line breaks) render correctly in alarm pages.</verify>
  <done>Alarm narrative block displays payload-rich content with better readability and no fixed section parser.</done>
</task>

<task type="code">
  <name>Add raw fallback path and styling</name>
  <files>frontend/charts/app.js, frontend/charts/styles/components.css</files>
  <action>Add UI fallback that displays raw text when markdown conversion yields empty/error output, and style narrative blocks for parity readability.</action>
  <verify>Fallback appears for malformed markdown payloads and remains legible.</verify>
  <done>Narrative rendering is resilient across payload variability.</done>
</task>

## Verification

- `node --check frontend/charts/app.js`
- Manual payload validation with sample prognosis markdown and malformed content fallback.

## Success Criteria

- [ ] Prognosis/description render as sanitized markdown.
- [ ] No fixed parsing assumptions are required for narrative payloads.
- [ ] Raw fallback path works when markdown rendering fails or is empty.
