---
phase: 05-optimizing-for-performance-and-speed
plan: 04
type: execute
wave: 2
depends_on: [05-01, 05-03]
files_modified:
  - backend/app/services/profiling_adapter.py
  - backend/app/api/charts.py
  - frontend/charts/services/api-client.js
autonomous: true
user_setup: []
must_haves:
  truths:
    - "Repeated item attributes/equipment sensor lookups should use TTL cache."
    - "Manual refresh must bypass cached chart query payloads for targeted queries."
  artifacts:
    - "backend/app/services/profiling_adapter.py"
    - "frontend/charts/services/api-client.js"
  key_links: []
---

# Phase 5 Plan 04: Caching and Validation Baseline

## Objective

- **What:** Add cache layers and baseline verification checks for performance behavior.
- **Why:** Repeated API queries were recomputing expensive data and chart refresh needed predictable cache semantics.
- **Output:** Backend TTL caches, frontend refresh-aware cache invalidation, and baseline endpoint/perf checks.

## Context

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

## Tasks

<task type="code">
  <name>Add backend TTL cache for attributes and equipment sensors</name>
  <files>backend/app/services/profiling_adapter.py</files>
  <action>Cache item attributes and equipment sensors with workspace-scoped TTL keys and return deep copies.</action>
  <verify>Repeated lookups in same runtime hit cache path and still return correct payload shape.</verify>
  <done>Repeated sensor/attribute requests are reduced in latency and backend load.</done>
</task>

<task type="code">
  <name>Implement frontend in-flight dedupe and targeted refresh cache bypass</name>
  <files>frontend/charts/services/api-client.js</files>
  <action>Add in-flight promise dedupe by query key and support force-refresh cache invalidation on manual refresh actions.</action>
  <verify>Repeated concurrent requests for same query share one network call; manual refresh bypasses cached value.</verify>
  <done>Client avoids duplicate fetches while still honoring explicit refresh semantics.</done>
</task>

<task type="test">
  <name>Record baseline technical verification evidence</name>
  <files>backend/app, frontend/charts/*.js</files>
  <action>Run compile/syntax checks and OpenAPI path validation for new contracts.</action>
  <verify>`compileall`, `node --check`, and OpenAPI checks pass locally.</verify>
  <done>Phase has reproducible technical verification evidence before UAT.</done>
</task>

## Verification

- `python -m compileall backend/app`
- `node --check frontend/charts/services/api-client.js`
- `node --check frontend/charts/state/store.js`
- `node --check frontend/charts/components/chart-card.js`

## Success Criteria

- [x] Backend caches attributes and sensor groups with TTL.
- [x] Frontend dedupes identical in-flight queries and bypasses cache on manual refresh.
