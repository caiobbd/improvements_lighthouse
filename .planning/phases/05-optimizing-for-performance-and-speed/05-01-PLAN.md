---
phase: 05-optimizing-for-performance-and-speed
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/api/charts.py
  - backend/app/services/profiling_adapter.py
  - backend/app/main.py
  - backend/app/config.py
autonomous: true
user_setup: []
must_haves:
  truths:
    - "/pages must return without blocking live timeseries validation loops."
    - "Equipment tree endpoint must support lean payload mode."
  artifacts:
    - "backend/app/api/charts.py"
    - "backend/app/services/profiling_adapter.py"
  key_links: []
---

# Phase 5 Plan 01: Startup and Tree Performance

## Objective

- **What:** Remove startup bottlenecks from preset page loading and reduce equipment tree transfer weight.
- **Why:** First usable paint and sidebar availability were delayed by blocking data work and large payloads.
- **Output:** Fast `/pages` behavior, lean `/equipment-tree`, gzip middleware, and backend tree/attribute cache support.

## Context

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

## Tasks

<task type="code">
  <name>Remove blocking preset validation from /pages</name>
  <files>backend/app/api/charts.py</files>
  <action>Stop calling timeseries validation loops while building preset charts; choose ranked attribute directly.</action>
  <verify>GET /api/v1/charts/pages responds without multi-attribute timeseries probe loop.</verify>
  <done>Preset pages load quickly and still include chart definitions.</done>
</task>

<task type="code">
  <name>Optimize equipment tree transport and backend startup middleware</name>
  <files>backend/app/api/charts.py, backend/app/services/profiling_adapter.py, backend/app/main.py, backend/app/config.py</files>
  <action>Add `lean` support to equipment tree responses, transform nodes to lean shape, and enable GZip middleware with configurable threshold.</action>
  <verify>OpenAPI includes `lean` query on /equipment-tree and app initializes with GZip middleware.</verify>
  <done>Tree endpoint returns lean-compatible payload and backend compresses large responses.</done>
</task>

## Verification

- `python -m compileall backend/app`
- Verify `/api/v1/charts/equipment-tree?lean=true` works on running backend

## Success Criteria

- [x] `/pages` no longer performs blocking timeseries validation loops.
- [x] `/equipment-tree` supports lean payload mode and backend gzip is enabled.
