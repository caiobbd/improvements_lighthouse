---
phase: 05-optimizing-for-performance-and-speed
plan: 02
type: execute
wave: 1
depends_on: [05-01]
files_modified:
  - frontend/charts/state/store.js
  - frontend/charts/components/page-controls.js
  - frontend/charts/app.js
autonomous: true
user_setup: []
must_haves:
  truths:
    - "Page-level frequency mode must support auto and manual override."
    - "Auto protocol must follow <=1d 15m, <=7d 1h, <=30d 6h, >30d 1d."
  artifacts:
    - "frontend/charts/state/store.js"
    - "frontend/charts/components/page-controls.js"
  key_links: []
---

# Phase 5 Plan 02: Frequency Protocol and User Control

## Objective

- **What:** Add page-level frequency state and UI control with Auto protocol and manual override.
- **Why:** Users need a macro-first default for speed with optional direct frequency control.
- **Output:** Persisted frequency state (`frequencyMode`, `frequencyWindow`) and a selector in page controls.

## Context

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

## Tasks

<task type="code">
  <name>Persist page-level frequency configuration</name>
  <files>frontend/charts/state/store.js</files>
  <action>Add frequency fields to page state, normalize persisted values, and expose `setFrequency` action.</action>
  <verify>State snapshot includes frequency mode/window and survives reload via localStorage.</verify>
  <done>Frequency settings are persisted and available to chart fetch logic.</done>
</task>

<task type="code">
  <name>Add frequency control to page controls row</name>
  <files>frontend/charts/components/page-controls.js, frontend/charts/app.js</files>
  <action>Add selector options (`Auto`, `15m`, `1h`, `6h`, `1d`) and wire to `setFrequency` action.</action>
  <verify>Changing selector updates active page state and triggers chart data refresh path.</verify>
  <done>User can switch between auto protocol and manual frequency override.</done>
</task>

## Verification

- `node --check frontend/charts/state/store.js`
- `node --check frontend/charts/components/page-controls.js`
- `node --check frontend/charts/app.js`

## Success Criteria

- [x] Frequency mode is stored at page-level as `auto|manual`.
- [x] UI supports Auto plus explicit frequency override values.
