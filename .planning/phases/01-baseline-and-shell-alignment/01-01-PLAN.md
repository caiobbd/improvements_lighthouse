---
phase: 01-baseline-and-shell-alignment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/main.py
  - backend/app/config.py
  - backend/app/models/charts.py
  - backend/app/services/workspace_client.py
  - backend/app/services/profiling_adapter.py
  - backend/app/api/charts.py
  - backend/requirements.txt
  - backend/.env.example
autonomous: true
requirements: [OVW-01, UXQ-01, UXQ-05]
user_setup:
  - service: workspace_api_access
    why: "Workspace API credentials are required to fetch live profiling data"
    env_vars:
      - name: WORKSPACE_BASE_URL
        source: "Workspace/Lighthouse environment URL"
      - name: WORKSPACE_API_KEY
        source: "Workspace API key"
      - name: WORKSPACE_ID
        source: "Workspace identifier (optional if auto-discovery is enabled)"
must_haves:
  truths:
    - "Backend exposes a running FastAPI service with chart-focused endpoints and health check."
    - "A chart timeseries endpoint returns normalized timestamp/value series payloads suitable for D3."
    - "Profiling adapter can resolve asset and timeseries using the workspace profiling reference module."
  artifacts:
    - path: "backend/app/main.py"
      provides: "FastAPI app entrypoint and router registration"
      min_lines: 30
      contains: "FastAPI("
    - path: "backend/app/api/charts.py"
      provides: "Charts endpoints for timeseries and page metadata operations"
      min_lines: 60
      exports: ["router"]
    - path: "backend/app/services/profiling_adapter.py"
      provides: "Adapter around ProfilingV1 and workspace APIs"
      min_lines: 80
      contains: "ProfilingV1"
    - path: "backend/app/models/charts.py"
      provides: "Pydantic response models for chart payloads"
      min_lines: 40
      contains: "BaseModel"
  key_links:
    - from: "backend/app/main.py"
      to: "backend/app/api/charts.py"
      via: "router include for charts endpoints"
      pattern: "include_router"
    - from: "backend/app/api/charts.py"
      to: "backend/app/services/profiling_adapter.py"
      via: "endpoint dependency invokes adapter methods"
      pattern: "ProfilingAdapter|profiling_adapter"
    - from: "backend/app/services/profiling_adapter.py"
      to: "C:/Users/CaioBorges/_pgms/shape-workspace-api/workspace_api/profiling2/core.py"
      via: "imports and wraps ProfilingV1 access patterns"
      pattern: "ProfilingV1"
---

<objective>
Create the FastAPI backend foundation for the Lighthouse Charts tab.

Purpose: establish a stable chart data API contract and isolate workspace profiling integration.
Output: runnable backend service, typed DTO schemas, and adapter-based chart endpoints.
</objective>

<execution_context>
@C:/Users/CaioBorges/_pgms/_repos/improvements_lighthouse/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/CaioBorges/_pgms/_repos/improvements_lighthouse/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-baseline-and-shell-alignment/01-CONTEXT.md
@.planning/phases/01-baseline-and-shell-alignment/01-RESEARCH.md
@C:/Users/CaioBorges/_pgms/shape-workspace-api/workspace_api/profiling2/core.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold FastAPI service and typed chart contracts</name>
  <files>backend/app/main.py, backend/app/config.py, backend/app/models/charts.py, backend/requirements.txt, backend/.env.example</files>
  <read_first>.planning/phases/01-baseline-and-shell-alignment/01-CONTEXT.md, .planning/phases/01-baseline-and-shell-alignment/01-RESEARCH.md</read_first>
  <action>Create backend app scaffold with FastAPI + CORS, app config loader for workspace API-key configuration, and Pydantic models for chart payloads (`SeriesPoint`, `Series`, `TimeSeriesResponse`, `ChartPage`, `ChartDefinition`). Define `.env.example` keys `WORKSPACE_BASE_URL`, `WORKSPACE_API_KEY`, and optional `WORKSPACE_ID`/`WORKSPACE_NUMBER`. Keep response models minimal and D3-friendly (ISO timestamps + numeric values).</action>
  <verify>python -m py_compile backend/app/main.py backend/app/models/charts.py backend/app/config.py</verify>
  <acceptance_criteria>
    - backend/app/main.py contains `FastAPI(` and includes a charts router import.
    - backend/app/models/charts.py defines `TimeSeriesResponse`.
    - backend/.env.example includes all required workspace env vars.
  </acceptance_criteria>
  <done>Backend scaffold compiles and exposes typed chart DTO definitions.</done>
</task>

<task type="auto">
  <name>Task 2: Implement profiling adapter around Workspace/ProfilingV1</name>
  <files>backend/app/services/workspace_client.py, backend/app/services/profiling_adapter.py</files>
  <read_first>C:/Users/CaioBorges/_pgms/shape-workspace-api/workspace_api/profiling2/core.py</read_first>
  <action>Implement a thin `workspace_client.py` for creating workspace sessions and a `profiling_adapter.py` wrapper with methods: `resolve_asset(asset_name)`, `list_chartable_attributes(asset_name)`, and `get_timeseries(asset_name, start_date, end_date, window, from_categories, from_attributes)`. Normalize dataframe output to list-of-series objects with `timestamp` and `value`, handling missing/empty data and explicit error messages for unknown assets.</action>
  <verify>python -m py_compile backend/app/services/workspace_client.py backend/app/services/profiling_adapter.py</verify>
  <acceptance_criteria>
    - backend/app/services/profiling_adapter.py contains `ProfilingV1` usage.
    - Adapter returns normalized JSON-serializable structures (no pandas objects in API layer).
    - Unknown asset path raises controlled API error shape (not raw traceback).
  </acceptance_criteria>
  <done>Adapter can produce chart-ready series from workspace profiling calls.</done>
</task>

<task type="auto">
  <name>Task 3: Expose chart endpoints and verify local service startup</name>
  <files>backend/app/api/charts.py, backend/app/main.py</files>
  <read_first>backend/app/models/charts.py, backend/app/services/profiling_adapter.py</read_first>
  <action>Create `/api/v1/charts/health`, `/api/v1/charts/timeseries`, and `/api/v1/charts/pages` endpoints. `/timeseries` accepts `asset_name`, `start_date`, `end_date`, `window`, optional attribute/category filters; `/pages` can return initial preset stub payload for frontend integration. Register router in `main.py` and ensure endpoint responses conform to Pydantic models.</action>
  <verify>uvicorn backend.app.main:app --host 127.0.0.1 --port 8001 --reload</verify>
  <acceptance_criteria>
    - `GET /api/v1/charts/health` returns success payload.
    - `GET /api/v1/charts/timeseries` route exists and returns typed response shape.
    - `backend/app/main.py` includes charts router under `/api/v1/charts`.
  </acceptance_criteria>
  <done>FastAPI backend runs with chart endpoints wired to adapter services.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -m py_compile backend/app/main.py backend/app/api/charts.py backend/app/services/profiling_adapter.py` passes
- [ ] Uvicorn startup command succeeds locally
- [ ] Endpoint contracts match Pydantic schema definitions
</verification>

<success_criteria>

- All tasks completed
- FastAPI backend provides stable chart-oriented endpoint contracts
- Workspace profiling integration is isolated behind adapter layer
- No untyped/raw dataframe objects leak to endpoint responses
</success_criteria>

<output>
After completion, create `.planning/phases/01-baseline-and-shell-alignment/01-01-SUMMARY.md`
</output>
