---
phase: 01-baseline-and-shell-alignment
plan: 04
type: execute
wave: 2
depends_on: [01-01, 01-02, 01-03]
files_modified:
  - backend/app/models/charts.py
  - backend/app/api/charts.py
  - backend/app/services/workspace_client.py
  - frontend/charts/services/api-client.js
  - frontend/charts/state/store.js
  - frontend/charts/components/chart-card.js
  - frontend/charts/app.js
autonomous: true
requirements: [OVW-01, UXQ-01, UXQ-05, UXQ-06]
must_haves:
  truths:
    - "Backend /pages returns at least one preset chart tied to real API asset metadata (item_id + attribute_id/attribute_name)."
    - "Frontend bootstraps preset pages from backend /pages instead of using only local hardcoded placeholders."
    - "Preset charts can fetch timeseries through attribute-based endpoint and produce non-empty points for at least one chart."
  artifacts:
    - path: "backend/app/api/charts.py"
      provides: "Dynamic preset-page assembly from live Workspace asset/attribute discovery"
      min_lines: 120
      contains: "preset-live"
    - path: "frontend/charts/app.js"
      provides: "Bootstrap flow that syncs backend preset pages into store"
      min_lines: 50
      contains: "getPages"
    - path: "frontend/charts/state/store.js"
      provides: "Preset-page synchronization and snake_case to camelCase normalization"
      min_lines: 180
      contains: "syncPresetPages"
  key_links:
    - from: "backend/app/api/charts.py"
      to: "backend/app/services/profiling_adapter.py"
      via: "preset charts resolve item/attributes and validate timeseries"
      pattern: "get_item_attributes|get_timeseries_from_attribute"
    - from: "frontend/charts/app.js"
      to: "frontend/charts/services/api-client.js"
      via: "bootstrap requests backend pages and syncs preset workspace state"
      pattern: "getPages"
    - from: "frontend/charts/components/chart-card.js"
      to: "frontend/charts/services/api-client.js"
      via: "chart card requests attribute-based timeseries when item/attribute identifiers exist"
      pattern: "attribute_id|attribute_name|item_id"
---

<objective>
Close the Phase 1 gap between API reachability and actual preset chart rendering with live data.

Purpose: ensure at least one pre-saved chart is operational without manual chart configuration.
Output: backend-driven preset chart definitions + frontend hydration + verified non-empty preset timeseries.
</objective>

<execution_context>
@C:/Users/CaioBorges/_pgms/_repos/improvements_lighthouse/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/CaioBorges/_pgms/_repos/improvements_lighthouse/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-baseline-and-shell-alignment/01-CONTEXT.md
@.planning/phases/01-baseline-and-shell-alignment/01-03-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build live preset chart metadata in backend pages endpoint</name>
  <files>backend/app/models/charts.py, backend/app/api/charts.py</files>
  <read_first>backend/app/services/profiling_adapter.py</read_first>
  <action>Extend chart definition schema with optional `item_id`, `attribute_id`, and `attribute_name`. Update `/api/v1/charts/pages` to build preset charts from real assets (`MV27-HBG-1130C (CRUDE HEATER)`, `MV30-HBG-1120A (CRUDE HEATER)`), selecting attributes with timeseries references and validating non-empty series where possible.</action>
  <verify>python -m py_compile backend/app/models/charts.py backend/app/api/charts.py</verify>
  <acceptance_criteria>
    - `/pages` returns preset charts with live `asset_name` values matching target assets.
    - At least one chart in payload includes `item_id` and `attribute_id`.
    - API falls back gracefully when live discovery is unavailable.
  </acceptance_criteria>
  <done>Preset pages are seeded by real API-resolved chart metadata.</done>
</task>

<task type="auto">
  <name>Task 2: Support attribute-targeted timeseries retrieval in chart cards</name>
  <files>frontend/charts/services/api-client.js, frontend/charts/components/chart-card.js</files>
  <read_first>backend/app/api/charts.py</read_first>
  <action>Update API client to route chart requests to `/timeseries-from-attribute` when `item_id` + (`attribute_id` or `attribute_name`) are present, while preserving `/timeseries` behavior for manual filters. Pass these fields from chart card fetch payloads.</action>
  <verify>node --check frontend/charts/services/api-client.js && node --check frontend/charts/components/chart-card.js</verify>
  <acceptance_criteria>
    - Chart-card requests include `item_id`/`attribute_id` when available.
    - Endpoint selection is deterministic and backward compatible.
    - Error handling remains non-crashing in UI.
  </acceptance_criteria>
  <done>Preset charts can load data deterministically from attribute-targeted API path.</done>
</task>

<task type="auto">
  <name>Task 3: Hydrate frontend preset pages from backend on app bootstrap</name>
  <files>frontend/charts/state/store.js, frontend/charts/app.js</files>
  <read_first>frontend/charts/state/store.js, frontend/charts/services/api-client.js</read_first>
  <action>Add store normalization/sync action for backend preset pages and wire app bootstrap to fetch `/pages` before regular rendering. Keep local defaults as fallback when backend is unavailable.</action>
  <verify>node --check frontend/charts/state/store.js && node --check frontend/charts/app.js</verify>
  <acceptance_criteria>
    - App attempts `getPages` at startup.
    - Preset pages are replaced/synced from backend payloads.
    - Existing custom pages remain usable after preset sync.
  </acceptance_criteria>
  <done>UI starts with backend-defined preset charts instead of static placeholders.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `/api/v1/charts/pages` returns at least one chart with `item_id` + `attribute_id`
- [ ] `/api/v1/charts/timeseries-from-attribute` returns non-empty points for at least one preset chart config
- [ ] Frontend compiles and can bootstrap backend presets without runtime errors
</verification>

<success_criteria>

- All tasks completed
- Phase 1 now includes functional API-backed preset visualization behavior
- At least one pre-saved chart can render non-empty real data without manual chart setup
</success_criteria>

<output>
After completion, create `.planning/phases/01-baseline-and-shell-alignment/01-04-SUMMARY.md`
</output>
