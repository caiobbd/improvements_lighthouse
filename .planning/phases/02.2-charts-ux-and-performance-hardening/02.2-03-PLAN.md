---
phase: 02.2-charts-ux-and-performance-hardening
plan: 03
type: execute
wave: 2
depends_on: [02.2-01, 02.2-02]
files_modified:
  - frontend/charts/services/api-client.js
  - frontend/charts/components/chart-card.js
  - frontend/charts/state/store.js
autonomous: true
requirements: [CHT-10, CHT-11]
must_haves:
  truths:
    - "Default timeseries sampling uses 6h window for faster initial chart loads."
    - "Recent chart data requests reuse temporary local cache when params match."
  artifacts:
    - path: "frontend/charts/services/api-client.js"
      provides: "Timeseries cache with deterministic request key and TTL"
      min_lines: 120
      contains: "cache"
    - path: "frontend/charts/components/chart-card.js"
      provides: "Chart load path defaults to 6h sampling when not explicitly set"
      min_lines: 260
      contains: "window"
  key_links:
    - from: "frontend/charts/components/chart-card.js"
      to: "frontend/charts/services/api-client.js"
      via: "6h-window timeseries calls routed through cache-enabled client"
      pattern: "getTimeSeries"
---

<objective>
Improve perceived chart load performance with 6h default sampling and temporary local cache.
</objective>

<execution_context>
@C:/Users/CaioBorges/_pgms/_repos/improvements_lighthouse/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/CaioBorges/_pgms/_repos/improvements_lighthouse/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02.2-charts-ux-and-performance-hardening/02.2-CONTEXT.md
@frontend/charts/services/api-client.js
@frontend/charts/components/chart-card.js
@frontend/charts/state/store.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Set 6h as chart default query window</name>
  <files>frontend/charts/state/store.js, frontend/charts/components/chart-card.js</files>
  <action>Change default sampling window from 1h to 6h in chart config and runtime fetch fallback paths.</action>
  <verify>node --check frontend/charts/state/store.js && node --check frontend/charts/components/chart-card.js</verify>
  <done>Charts request fewer points by default for improved load speed.</done>
</task>

<task type="auto">
  <name>Task 2: Add temporary local cache for timeseries requests</name>
  <files>frontend/charts/services/api-client.js</files>
  <action>Implement request-keyed in-memory cache (with short TTL) for timeseries payloads to accelerate repeated chart loads.</action>
  <verify>node --check frontend/charts/services/api-client.js</verify>
  <done>Repeated requests with identical params reuse recent response data.</done>
</task>

<task type="auto">
  <name>Task 3: Validate cache behavior against date/filter changes</name>
  <files>frontend/charts/services/api-client.js, frontend/charts/components/chart-card.js</files>
  <action>Ensure cache key includes window/date/filter context so stale data is not reused across different chart query intents.</action>
  <verify>findstr /C:"start_date" frontend\\charts\\services\\api-client.js</verify>
  <done>Cache improves speed while preserving query correctness.</done>
</task>

</tasks>

<verification>
- [ ] New charts default to 6h sampling
- [ ] Back-to-back identical chart loads hit cache
- [ ] Changed date range/preset invalidates cache key naturally
</verification>

<output>
After completion, create `.planning/phases/02.2-charts-ux-and-performance-hardening/02.2-03-SUMMARY.md`
</output>
