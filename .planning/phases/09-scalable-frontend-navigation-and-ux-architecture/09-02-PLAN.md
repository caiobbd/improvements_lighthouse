---
phase: 09-scalable-frontend-navigation-and-ux-architecture
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - frontend/charts/components/tab-navigation.js
  - frontend/charts/components/page-controls.js
  - frontend/charts/components/date-filter.js
  - frontend/charts/styles/charts-page.css
  - frontend/charts/styles/components.css
autonomous: true
requirements:
  - TBD
user_setup: []
must_haves:
  truths:
    - "Charts tabs row remains separate from charts controls row."
    - "Top-right controls keep quick ranges, reload/frequency behavior, and compact operator-first ergonomics."
    - "Grid controls are icon-only with exactly `1-col` and `2-col` modes."
    - "Settings gear menu keeps exact six locked actions and excludes System Info."
    - "No legacy duplicated top controls are visible."
  artifacts:
    - path: "frontend/charts/components/tab-navigation.js"
      provides: "Dedicated tabs row with trailing `+` page creation behavior"
    - path: "frontend/charts/components/page-controls.js"
      provides: "Controls row exactness, grid mode lock (1/2), and settings menu contract"
    - path: "frontend/charts/components/date-filter.js"
      provides: "Locked quick-range labels and custom range behavior"
    - path: "frontend/charts/styles/charts-page.css"
      provides: "Row-level compact spacing/alignment between left and right control bands"
    - path: "frontend/charts/styles/components.css"
      provides: "Narrow, deterministic styles for tab/control affordances"
  key_links:
    - from: "tabs row"
      to: "page creation"
      via: "trailing `+` tab after last page tab"
      pattern: "addPage"
    - from: "controls row"
      to: "date filter + frequency + grid + settings"
      via: "single compact control band"
      pattern: "24H|1W|4W|90D|Custom|frequency|grid"
---

# Phase 9 Plan 02: Tabs/Controls Exactness Under the New Shell

## Objective

- **What:** Finalize Charts row-level controls behavior under the rebaselined app-header shell.
- **Why:** The rebaseline changed shell hierarchy and grid contract; controls must now align with `1-col/2-col`, no-metadata, and compact row requirements.
- **Output:** Deterministic tabs+controls contract ready for chart-card behavior/density finish in 09-03.

## Scope Boundary

This plan owns only tab row and controls row exactness, including grid mode lock and gear action contract.

This plan does not modify header shell architecture from 09-01 or chart-card help/menu interaction logic from 09-03.

## Context

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-scalable-frontend-navigation-and-ux-architecture/09-CONTEXT.md
@.planning/phases/09-scalable-frontend-navigation-and-ux-architecture/09-01-SUMMARY.md

<defects_to_fix>
- Existing control assumptions still reference old grid modes (`2-col/3-col`) and stale metadata expectations.
- Tabs/controls compactness and alignment must be validated against the new shell spacing.
- Legacy duplicate controls must remain absent after shell rebaseline.
</defects_to_fix>

## Tasks

<task type="auto">
  <name>Task 1: Preserve dedicated charts tabs row and add-page tab behavior</name>
  <files>frontend/charts/components/tab-navigation.js, frontend/charts/styles/components.css</files>
  <action>Keep page tabs isolated in their own row under the app header. Ensure trailing `+` tab remains immediately after the last page tab and wired to page creation. Keep active state, dirty marker, truncation, and page switching behavior intact. Ensure no extra `New Page` top button appears.</action>
  <verify>
    <automated>node --check frontend/charts/components/tab-navigation.js</automated>
    <manual>Confirm tabs row is separate from controls row and `+` creates a new page from the trailing slot.</manual>
  </verify>
  <done>Tabs row remains deterministic and compact under the new shell.</done>
</task>

<task type="auto">
  <name>Task 2: Lock controls row contract with 1-col/2-col grid modes</name>
  <files>frontend/charts/components/page-controls.js, frontend/charts/components/date-filter.js, frontend/charts/styles/charts-page.css, frontend/charts/styles/components.css</files>
  <action>Keep quick ranges (`24H`, `1W`, `4W`, `90D`, `Custom`), reload/frequency coupling, and compact controls-row alignment. Update grid controls to icon-only `1-col` and `2-col` modes only. Keep controls row visually aligned with left-pane filter/search band where applicable.</action>
  <verify>
    <automated>node --check frontend/charts/components/page-controls.js</automated>
    <automated>node --check frontend/charts/components/date-filter.js</automated>
    <manual>Confirm controls row has quick ranges, reload, frequency toggle/dropdown behavior, and exactly two grid icons for 1/2 columns.</manual>
  </verify>
  <done>Controls row matches the rebaselined compact contract and updated grid policy.</done>
</task>

<task type="auto">
  <name>Task 3: Keep exact settings action set and metadata hidden</name>
  <files>frontend/charts/components/page-controls.js, frontend/charts/styles/components.css</files>
  <action>Enforce exact settings menu actions: `Columns selector`, `Add chart`, `Save page`, `Rename page`, `Duplicate page`, `Delete page`. Keep `System Info`, `Environment`, and `Backend Contract` out of visible controls/UI in this phase. Ensure no legacy duplicate controls reappear.</action>
  <verify>
    <automated>node --check frontend/charts/components/page-controls.js</automated>
    <automated>powershell -NoProfile -Command "$bad = rg -n 'System Info|Environment|Backend Contract|New Page|Refresh Data' frontend/charts/components/page-controls.js frontend/charts/components/tab-navigation.js frontend/charts/index.html; if ($LASTEXITCODE -eq 0) { $bad; exit 1 } else { exit 0 }"</automated>
    <manual>Open settings gear and validate exact six actions; confirm no metadata controls are visible in header/tabs/controls surfaces.</manual>
  </verify>
  <done>Settings and metadata visibility match the rebaselined Phase 9 scope.</done>
</task>

## Verification

- Tabs row and controls row are distinct and compact.
- Trailing `+` tab behavior remains correct.
- Quick ranges and reload/frequency behavior remain correct.
- Grid modes are icon-only and limited to `1-col`/`2-col`.
- Settings menu contains exactly six locked actions.
- `System Info`/metadata are hidden from visible UI.
- No legacy duplicate top controls are reintroduced.

Run:

- `node --check frontend/charts/components/tab-navigation.js`
- `node --check frontend/charts/components/page-controls.js`
- `node --check frontend/charts/components/date-filter.js`
- `powershell -NoProfile -Command "$bad = rg -n 'System Info|Environment|Backend Contract|New Page|Refresh Data' frontend/charts/components/page-controls.js frontend/charts/components/tab-navigation.js frontend/charts/index.html; if ($LASTEXITCODE -eq 0) { $bad; exit 1 } else { exit 0 }"`

## Success Criteria

- [ ] Tabs and controls remain separated into dedicated rows.
- [ ] `+` tab creates page from the trailing position.
- [ ] Controls row keeps quick-range and reload/frequency behavior.
- [ ] Grid exposes only `1-col` and `2-col` modes.
- [ ] Settings gear retains exact six actions.
- [ ] Metadata and System Info remain hidden for this phase.
- [ ] 09-03 can focus strictly on chart interaction isolation and compact chart chrome.

## Output

After completion, create `.planning/phases/09-scalable-frontend-navigation-and-ux-architecture/09-02-SUMMARY.md`.
