---
phase: 09-scalable-frontend-navigation-and-ux-architecture
plan: 03
type: execute
wave: 3
depends_on:
  - 09-02
files_modified:
  - frontend/charts/components/chart-card.js
  - frontend/charts/styles/components.css
  - frontend/charts/styles/charts-page.css
autonomous: true
requirements:
  - TBD
user_setup: []
must_haves:
  truths:
    - "Chart header controls are icon-only, ordered as help leftmost, refresh, then actions."
    - "Hovering or focusing `?` opens only help and never opens the `...` menu."
    - "`...` menu opens only from the `...` trigger, and help tooltip and action menu cannot be open at the same time."
    - "Chart chrome is reduced while plot drawing height remains unchanged and readable."
    - "All Phase 9 acceptance gate items pass together after shell and top-control remediation."
  artifacts:
    - path: "frontend/charts/components/chart-card.js"
      provides: "Button-scoped chart help, refresh, and action menu behavior"
    - path: "frontend/charts/styles/components.css"
      provides: "Narrow chart-card control, overlay, and chrome-density styles"
    - path: "frontend/charts/styles/charts-page.css"
      provides: "Page-level chart grid density adjustments only where needed"
  key_links:
    - from: "`?` help trigger"
      to: "help tooltip"
      via: "help-only state that closes actions menu first"
      pattern: "help"
    - from: "`...` action trigger"
      to: "action menu"
      via: "menu-only state that closes help tooltip first"
      pattern: "menu"
    - from: "chart-card density CSS"
      to: "plot drawing height"
      via: "reduce surrounding chrome without changing `renderLineChart({ height: 300 })`"
      pattern: "height: 300"
---

# Phase 9 Plan 03: Chart Overlay Isolation, Density, and Acceptance Verification

## Objective

- **What:** Fix chart-card interaction leakage, reduce chart chrome, and run the final locked acceptance gate.
- **Why:** The latest review explicitly rejected the help/actions overlap bug and worsened vertical density; those failures must be closed after shell and top controls are stable.
- **Output:** A strict remediation pass proving Phase 9 meets the locked acceptance gate without product-scope drift.

## Scope Boundary

This plan owns only chart-card header controls, help/actions overlay mutual exclusion, chart-card chrome density, plot-height preservation checks, and final verification.

This plan must not change shell/sidebar structure, System Info placement, top tab/control composition, gear-menu contents, quick-range behavior, or right-sidebar visibility. If a gate failure is found in 09-01 or 09-02 scope during final verification, record it in the summary and fix it only by reopening the owning plan scope, not by mixing unrelated edits into chart-card remediation.

## Context

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-scalable-frontend-navigation-and-ux-architecture/09-CONTEXT.md
@.planning/phases/09-scalable-frontend-navigation-and-ux-architecture/09-RESEARCH.md
@.planning/phases/09-scalable-frontend-navigation-and-ux-architecture/09-01-SUMMARY.md
@.planning/phases/09-scalable-frontend-navigation-and-ux-architecture/09-02-SUMMARY.md

<defects_to_fix>
- Chart action menu currently opens from the whole `.chart-actions` cluster instead of only from `...`.
- Hovering/focusing `?` can open the `...` menu because menu state is owned by the cluster and CSS `:focus-within` leakage.
- Help tooltip and action menu can overlap visually/behaviorally; they must be mutually exclusive.
- Chart header controls must be icon-only, with `?` leftmost and hover highlight isolated to the hovered icon button.
- Vertical space usage worsened due to stacked rows/duplicated controls; chart chrome must shrink while preserving current plot drawing height.
</defects_to_fix>

## Tasks

<task type="auto">
  <name>Task 1: Scope chart header interactions to their owning icon buttons</name>
  <files>frontend/charts/components/chart-card.js, frontend/charts/styles/components.css</files>
  <action>Refactor chart header actions so the icon order is help `?` leftmost, refresh, then `...`. Keep controls icon-only and accessible with labels/tooltips that do not add visible text buttons. Move action-menu open/close ownership to the `...` trigger and menu only. Remove or neutralize any `.chart-actions` cluster-level `mouseenter`, `focusin`, or CSS `:focus-within` behavior that opens the menu. Ensure hover/focus highlight applies only to the individual hovered/focused icon button.</action>
  <verify>
    <automated>node --check frontend/charts/components/chart-card.js</automated>
    <manual>Hover and focus `?`: only help opens and `...` menu stays closed. Hover/click/focus refresh: only refresh highlight/action occurs. Hover/click/focus `...`: only action menu opens. Hover highlight never spills to sibling icons.</manual>
  </verify>
  <done>Rejected chart-control leakage defect is fixed and chart controls remain icon-only in the locked order.</done>
</task>

<task type="auto">
  <name>Task 2: Make help tooltip and action menu mutually exclusive</name>
  <files>frontend/charts/components/chart-card.js, frontend/charts/styles/components.css</files>
  <action>Introduce explicit mutually exclusive overlay state for chart help and chart actions. Opening help must close the `...` menu first. Opening the `...` menu must close help first. Escape, outside click, blur/leave behavior, and chart-card rerender cleanup must not leave both overlays open or leave orphan open CSS classes. Do not change action-menu contents except where required by existing behavior preservation.</action>
  <verify>
    <automated>node --check frontend/charts/components/chart-card.js</automated>
    <manual>Open help, then open `...` and confirm help closes. Open `...`, then open help and confirm menu closes. Press Escape/outside click and confirm overlays close cleanly. Repeat after chart refresh/rerender.</manual>
  </verify>
  <done>Help tooltip and actions menu cannot be open simultaneously and neither overlay opens from the wrong trigger.</done>
</task>

<task type="auto">
  <name>Task 3: Reduce chart chrome while preserving plot drawing height and run final acceptance gate</name>
  <files>frontend/charts/styles/components.css, frontend/charts/styles/charts-page.css, frontend/charts/components/chart-card.js</files>
  <action>Reduce non-essential card chrome: header/footer padding, action cluster footprint, legend/context default footprint, and dead margins around the chart body. Preserve axis readability, legend/context availability, cursor/zoom/threshold affordances, and the existing plot drawing height; do not reduce `renderLineChart({ height: 300 })` or equivalent chart draw-height contract. After density edits, run the full Phase 9 acceptance matrix across 09-01, 09-02, and 09-03 gate items.</action>
  <verify>
    <automated>node --check frontend/charts/components/chart-card.js</automated>
    <automated>rg -n "height:\\s*300|height:\\s*,?\\s*300|renderLineChart" frontend/charts/components/chart-card.js frontend/charts/components/d3-line-chart.js</automated>
    <automated>python -m pytest tests/frontend</automated>
    <manual>Compare before/after visually: plot drawing height remains unchanged, surrounding chrome is tighter, axes remain readable, and legends/context remain available. Then execute the final acceptance checklist in this plan's Verification section.</manual>
  </verify>
  <done>Plot drawing height is preserved, surrounding chrome is visibly reduced, existing chart interactions still work, and the final Phase 9 acceptance matrix passes.</done>
</task>

## Verification

Final locked acceptance gate:

- **Tabs row is browser-like and isolated from toolbar controls:** open Charts and confirm row separation established by 09-02.
- **`+` tab creates new page and is immediately after last tab:** click trailing `+`, confirm page creation.
- **Top-right row exactness:** confirm `24H`, `1W`, `4W`, `90D`, `Custom`, reload icon, frequency toggle, conditional manual dropdown, grid icons, and gear are present.
- **Gear menu exactness:** confirm exactly `Columns selector`, `Add chart`, `Save page`, `Rename page`, `Duplicate page`, `Delete page`; no `System Info` item.
- **No legacy duplicated top controls:** confirm no visible top `New Page`, `Actions`, inline `Edit`, inline `X`, old `Refresh Data`, old date-input preset row, or extra grid modes.
- **Chart header icon controls:** confirm chart header controls are icon-only, with `?` leftmost, refresh next, and `...` action menu trigger last.
- **Help/menu isolation:** hover/focus `?` and confirm it does not trigger `...`; open `...` and confirm it opens only from `...`; verify help and actions cannot remain open simultaneously.
- **Plot height and density:** confirm `renderLineChart({ height: 300 })` or equivalent draw-height contract remains unchanged while header/footer/internal chrome is reduced.
- **Regression smoke:** add/remove chart, sensor drag/drop plotting, refresh/loading continuity, settings actions, alarm/event sidebar toggles, and existing page persistence still work.

Run:

- `node --check frontend/charts/app.js`
- `node --check frontend/charts/components/tab-navigation.js`
- `node --check frontend/charts/components/page-controls.js`
- `node --check frontend/charts/components/date-filter.js`
- `node --check frontend/charts/components/chart-card.js`
- `python -m pytest tests/frontend`

## Success Criteria

- [ ] Rejected chart leakage defect is fixed: `?` never opens `...`, and `...` opens only from the `...` trigger.
- [ ] Help tooltip and action menu are mutually exclusive across hover, focus, click, Escape, outside click, and rerender paths.
- [ ] Chart header controls are icon-only with isolated hover/focus highlighting and locked order.
- [ ] Plot drawing height remains unchanged while chart-card chrome and dead margins are reduced.
- [ ] Final acceptance gate confirms shell/sidebar boundaries from 09-01, top controls from 09-02, and chart overlay/density from 09-03 all pass together.
- [ ] No README or unrelated documentation/product-scope edits are required for this remediation pass.

## Output

After completion, create `.planning/phases/09-scalable-frontend-navigation-and-ux-architecture/09-03-SUMMARY.md`.
