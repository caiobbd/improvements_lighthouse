---
phase: 08.1-improving-context-from-attributes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/requirements-dev.txt
  - tests/conftest.py
  - tests/backend/test_sensor_context_normalization.py
  - tests/backend/test_avg_1d_timeseries_only.py
  - tests/backend/test_sensor_context_api.py
  - tests/frontend/test_table_columns_state.py
  - backend/app/models/charts.py
  - backend/app/services/profiling_adapter.py
  - backend/app/api/charts.py
autonomous: true
requirements:
  - CHT-86
  - CHT-87
  - CHT-88
user_setup: []
must_haves:
  truths:
    - "Sensor-context data is enriched from attributes + sub_attributes + timeseries-derived values."
    - "Duplicate sub_attribute normalization is deterministic and follows locked tie-breakers."
    - "last_value and -1d AVG are computed from chart-window/timeseries semantics, with N/A-compatible outputs."
  artifacts:
    - "backend/app/api/charts.py"
    - "backend/app/services/profiling_adapter.py"
    - "backend/app/models/charts.py"
    - "tests/backend/test_sensor_context_normalization.py"
    - "tests/backend/test_avg_1d_timeseries_only.py"
  key_links:
    - "backend/app/api/charts.py -> backend/app/services/profiling_adapter.py via sensor-context endpoint handler"
    - "tests/backend/*.py -> normalization and avg computation helpers"
---

<objective>
Build Wave 0 validation scaffolding and backend sensor-context foundations for Phase 08.1.

Purpose: Deliver the locked backend-first sequencing and deterministic data contract before UI thresholds/custom-alarm UX.
Output: Test scaffolding plus a new backend sensor-context API path that returns enriched, normalized rows.
</objective>

<execution_context>
@C:/Users/CaioBorges/_pgms/_repos/improvements_lighthouse/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/CaioBorges/_pgms/_repos/improvements_lighthouse/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.1-improving-context-from-attributes/08.1-CONTEXT.md
@.planning/phases/08.1-improving-context-from-attributes/08.1-RESEARCH.md
@.planning/phases/08.1-improving-context-from-attributes/08.1-VALIDATION.md

<decision_map>
- D-01: Backend/table foundation must ship before threshold lines and custom-alarm UI.
- D-03: Missing context values must render as N/A.
- D-04: `last value` and `-1d AVG` semantics are locked to timeseries rules.
- D-05: Duplicate `sub_attributes` normalization order is locked.
</decision_map>

<interfaces>
From `backend/app/api/charts.py`:
- `@router.get("/item-attributes", response_model=ItemAttributesResponse)`
- `@router.get("/timeseries-batch", response_model=TimeSeriesBatchResponse)`

From `backend/app/services/profiling_adapter.py`:
- `def get_item_attributes(self, item_id: str, asset_name: str | None = None) -> list[ItemAttribute]`
- `def get_timeseries_from_attribute(...) -> list[Series]`

From `backend/app/models/charts.py`:
- `class ItemAttributesResponse(BaseModel)`
- `class TimeSeriesBatchResponse(BaseModel)`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 validation bootstrap for Phase 08.1</name>
  <files>backend/requirements-dev.txt, tests/conftest.py, tests/backend/test_sensor_context_normalization.py, tests/backend/test_avg_1d_timeseries_only.py, tests/backend/test_sensor_context_api.py, tests/frontend/test_table_columns_state.py</files>
  <action>Create pytest bootstrap files required by `08.1-VALIDATION.md`, including backend fixtures and placeholder frontend-state test scaffolds, so all downstream tasks have executable automated checks (per D-01).</action>
  <verify>
    <automated>python -m pytest --collect-only -q</automated>
  </verify>
  <done>All Wave 0 test modules listed in validation strategy are importable and discoverable via pytest collection.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement sensor-context normalization and timeseries-derived metrics</name>
  <files>backend/app/services/profiling_adapter.py, backend/app/models/charts.py, tests/backend/test_sensor_context_normalization.py, tests/backend/test_avg_1d_timeseries_only.py</files>
  <behavior>
    - Test 1: Duplicate threshold candidates resolve by non-null value, then `is_external=false`, then newest `updated_at` (per D-05).
    - Test 2: `-1d AVG` uses timeseries points only and never `sub_attributes` values (per D-04).
    - Test 3: Missing fields map to `N/A`-compatible null outputs for frontend fallback rendering (per D-03).
  </behavior>
  <action>Add reusable backend helpers that produce canonical sensor-context rows with `last_value`, `avg_1d`, threshold metadata, and deterministic duplicate resolution; do not introduce per-user personalization logic or deferred export/import behavior.</action>
  <verify>
    <automated>python -m pytest tests/backend/test_sensor_context_normalization.py tests/backend/test_avg_1d_timeseries_only.py -q</automated>
  </verify>
  <done>Normalization and metric rules are encoded in backend logic and validated by targeted tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Expose backend sensor-context batch endpoint contract</name>
  <files>backend/app/api/charts.py, backend/app/models/charts.py, tests/backend/test_sensor_context_api.py</files>
  <behavior>
    - Test 1: `sensor-context-batch` returns one row per tag with enriched fields and warnings list.
    - Test 2: Invalid/empty tags payload returns 422 with deterministic error.
    - Test 3: Endpoint response preserves chart window semantics for `last_value` and `avg_1d`.
  </behavior>
  <action>Add a dedicated API contract for table-ready sensor context (new endpoint and models), leaving existing timeseries endpoints stable for chart rendering consumers (per D-01 and D-04).</action>
  <verify>
    <automated>python -m pytest tests/backend/test_sensor_context_api.py -q</automated>
  </verify>
  <done>Backend provides a stable sensor-context contract that frontend table plans can consume without altering current chart-series API behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> `/api/v1/charts/sensor-context-batch` | Untrusted query payload and tag objects enter backend parser. |
| charts API -> workspace-derived attribute payloads | Raw attribute/sub_attribute data can be inconsistent or partially missing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.1-01 | T | `sensor-context` normalization | mitigate | Enforce locked tie-break order in a single helper with explicit tests for precedence (D-05). |
| T-08.1-02 | D | `sensor-context-batch` request parser | mitigate | Validate non-empty tag list and reject malformed payload early with 422 to avoid expensive backend fan-out. |
| T-08.1-03 | I | optional context fields | mitigate | Return normalized contract fields only; keep raw internal payload details out of default response shape. |
</threat_model>

<verification>
- `python -m pytest --collect-only -q`
- `python -m pytest tests/backend/test_sensor_context_normalization.py tests/backend/test_avg_1d_timeseries_only.py tests/backend/test_sensor_context_api.py -q`
- `python -m pytest -q`
</verification>

<success_criteria>
- Wave 0 test bootstrap exists and runs in CI/local pytest collection.
- Backend sensor-context row enrichment supports locked semantics (`last_value`, `-1d AVG`, duplicate normalization).
- Dedicated sensor-context endpoint is available for frontend table foundation plans.
</success_criteria>

<output>
After completion, create `.planning/phases/08.1-improving-context-from-attributes/08.1-01-SUMMARY.md`
</output>
