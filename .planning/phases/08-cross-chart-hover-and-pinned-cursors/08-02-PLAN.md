---
phase: 08-cross-chart-hover-and-pinned-cursors
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - frontend/charts/components/d3-line-chart.js
  - frontend/charts/components/chart-card.js
  - frontend/charts/styles/components.css
autonomous: true
user_setup: []
must_haves:
  truths:
    - "Hover renders only a vertical line across charts and transient tooltip on active chart."
    - "Pinned cursors render opaque tooltip with UTC timestamp header and color-matched value rows."
    - "Values use nearest point only (no interpolation), and only visible traces are listed."
  artifacts:
    - "frontend/charts/components/d3-line-chart.js"
    - "frontend/charts/styles/components.css"
  key_links: []
---

# Phase 8 Plan 02: Hover and Pinned Tooltip Rendering

## Objective

- **What:** Implement synchronized hover line and pinned cursor visualization with Lighthouse-style tooltip hierarchy.
- **Why:** Operators need precise cross-chart time slicing and readable per-trace values at a consistent timestamp.
- **Output:** Reference-aligned hover/pin visuals with nearest-point value extraction and UTC display.

## Context

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-cross-chart-hover-and-pinned-cursors/08-CONTEXT.md

## Tasks

<task type="code">
  <name>Implement transient hover line and tooltip</name>
  <files>frontend/charts/components/d3-line-chart.js, frontend/charts/styles/components.css</files>
  <action>Render vertical hover line in all synced charts; on active chart render semi-transparent hover tooltip with UTC timestamp and nearest-point values for visible traces.</action>
  <verify>Moving mouse updates line in all charts and tooltip values update without interpolation.</verify>
  <done>Hover state is visual/transient and does not persist to pinned list.</done>
</task>

<task type="code">
  <name>Implement pinned cursor rendering with cap</name>
  <files>frontend/charts/components/d3-line-chart.js, frontend/charts/components/chart-card.js, frontend/charts/styles/components.css</files>
  <action>On click create pinned cursor up to max 5/page, render opaque tooltip anchored near top of line with timestamp header and per-trace values in trace colors.</action>
  <verify>First five clicks create pins; sixth click ignored until a pin is removed.</verify>
  <done>Pinned cursor visuals are stable and synchronized across charts.</done>
</task>

<task type="code">
  <name>Snap all cursor values to closest timestamp</name>
  <files>frontend/charts/components/d3-line-chart.js</files>
  <action>Use nearest available point per visible trace for hover/pinned values and timestamps; do not interpolate missing samples.</action>
  <verify>Tooltip values match closest sample points and hidden traces are excluded.</verify>
  <done>Nearest-point behavior is deterministic for hover and pins.</done>
</task>

## Verification

- `node --check frontend/charts/components/d3-line-chart.js`
- `node --check frontend/charts/components/chart-card.js`
- Manual: hover + pin on multi-trace chart, verify UTC timestamps and color-matched rows.

## Success Criteria

- [ ] Hover line synchronizes across charts while tooltip remains transient.
- [ ] Pinned tooltip structure matches required hierarchy (timestamp header + value rows).
- [ ] Nearest-point (non-interpolated) values are used for visible traces only.
