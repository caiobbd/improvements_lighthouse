---
phase: 08-cross-chart-hover-and-pinned-cursors
plan: 03
type: execute
wave: 2
depends_on:
  - 08-01
  - 08-02
files_modified:
  - frontend/charts/components/d3-line-chart.js
  - frontend/charts/styles/components.css
autonomous: true
user_setup: []
must_haves:
  truths:
    - "Pinned cursors can be dragged/repositioned or deleted by dragging off chart bounds."
    - "Cursor affordance changes to horizontal move when hovering draggable cursor area."
    - "Bottom 20% pan zone uses hand cursor and pans like Shift+drag."
  artifacts:
    - "frontend/charts/components/d3-line-chart.js"
    - "frontend/charts/styles/components.css"
  key_links: []
---

# Phase 8 Plan 03: Cursor Drag Lifecycle and Improved Panning Zone

## Objective

- **What:** Add draggable pinned cursor lifecycle and bottom-band pan ergonomics.
- **Why:** Users need direct cursor manipulation and faster panning without modifier keys.
- **Output:** Smooth drag-to-reposition/remove pins plus bottom 20% pan-zone behavior.

## Context

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-cross-chart-hover-and-pinned-cursors/08-CONTEXT.md

## Tasks

<task type="code">
  <name>Add pinned cursor drag-and-drop lifecycle</name>
  <files>frontend/charts/components/d3-line-chart.js</files>
  <action>Allow pointer drag on pinned line or tooltip header; reposition pin on in-bounds release and immediately delete when dragged beyond left/right chart bounds.</action>
  <verify>Drag inside chart moves pin; drag off left/right removes pin and frees a slot.</verify>
  <done>Pinned cursor drag lifecycle works with consistent cross-chart synchronization.</done>
</task>

<task type="code">
  <name>Add cursor affordances for draggable pins</name>
  <files>frontend/charts/components/d3-line-chart.js, frontend/charts/styles/components.css</files>
  <action>When hovering pin draggable area, show horizontal move cursor (`ew-resize`) and maintain clear active-drag visual feedback.</action>
  <verify>Hovering pin area changes cursor and active drag state is visibly distinct.</verify>
  <done>Cursor affordance communicates move interaction clearly.</done>
</task>

<task type="code">
  <name>Implement bottom 20% pan zone</name>
  <files>frontend/charts/components/d3-line-chart.js, frontend/charts/styles/components.css</files>
  <action>Define bottom interaction band (above x-axis) that shows hand cursor and routes drag to horizontal pan mode equivalent to `Shift+drag`.</action>
  <verify>Dragging in bottom band pans x-domain even without holding Shift; other areas keep existing zoom behaviors.</verify>
  <done>Pan-zone interaction improves scrub usability without breaking modifier-based controls.</done>
</task>

## Verification

- `node --check frontend/charts/components/d3-line-chart.js`
- Manual: drag pin inside/outside bounds, verify delete and cap slot release.
- Manual: confirm bottom-band pan works and does not interfere with click-to-pin.

## Success Criteria

- [ ] Pinned cursors support drag-reposition and drag-off-edge delete.
- [ ] Drag affordance uses `ew-resize` over cursor drag targets.
- [ ] Bottom 20% band provides hand-cursor pan mode equivalent to Shift+drag.
