---
phase: 08-cross-chart-hover-and-pinned-cursors
plan: 05
type: execute
wave: 4
depends_on:
  - 08-04
files_modified:
  - frontend/charts/components/d3-line-chart.js
  - frontend/charts/styles/components.css
autonomous: true
gap_closure: true
requirements:
  - CHT-78
  - CHT-79
  - CHT-80
must_haves:
  truths:
    - "Dragging a pinned cursor beyond the left/right chart edge removes the pin without selecting surrounding text."
    - "Pinned cursor drag remains clean and deterministic for in-bounds reposition and off-edge deletion."
    - "Horizontal move affordance remains visible while drag behavior prevents browser text-selection side effects."
  artifacts:
    - "frontend/charts/components/d3-line-chart.js"
    - "frontend/charts/styles/components.css"
  key_links:
    - from: "frontend/charts/components/d3-line-chart.js"
      to: "document/body selection state"
      via: "pin-drag lifecycle start/move/end cleanup"
      pattern: "pointerdown|pointermove|pointerup|pointercancel"
---

<objective>
Close the remaining Phase 08 UAT gap from Test 5 by preventing accidental browser text selection while dragging pinned cursors, especially during off-edge delete.

Purpose: Keep drag-off-edge removal behavior operationally clean and friction-free without regressing existing cursor interactions.
Output: Pointer-lifecycle guards and style hooks that suppress selection artifacts only during active pinned-cursor drag.
</objective>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-cross-chart-hover-and-pinned-cursors/08-CONTEXT.md
@.planning/phases/08-cross-chart-hover-and-pinned-cursors/08-UAT.md
@.planning/phases/08-cross-chart-hover-and-pinned-cursors/08-03-SUMMARY.md
@.planning/phases/08-cross-chart-hover-and-pinned-cursors/08-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add pinned-drag text-selection suppression lifecycle</name>
  <files>frontend/charts/components/d3-line-chart.js</files>
  <read_first>
    - frontend/charts/components/d3-line-chart.js
    - .planning/phases/08-cross-chart-hover-and-pinned-cursors/08-UAT.md
  </read_first>
  <action>
    Implement a dedicated selection-lock helper in `d3-line-chart.js` for pinned drag mode, and wire it to all pin-drag entry/exit paths.
    - On pinned-cursor drag start (`pinTarget` branch in `onPointerDown`), call `event.preventDefault()`, clear any active selection, and enable selection lock.
    - During pinned drag in `onPointerMove`, keep `event.preventDefault()` active and clear transient selection ranges if they appear.
    - On every pin-drag termination path (in-bounds release, off-edge delete, pointer cancel, or unexpected abort), always disable selection lock in a `finally`-safe cleanup path so normal page selection returns immediately after drag.
    - Preserve existing pin semantics from Phase 8 decisions: in-bounds drag repositions pin, off-edge drag removes pin, click-vs-drag arbitration unchanged.
  </action>
  <verify>
    <automated>node --check frontend/charts/components/d3-line-chart.js</automated>
    <automated>rg -n "preventDefault|removeAllRanges|selection|pin-drag" frontend/charts/components/d3-line-chart.js</automated>
  </verify>
  <acceptance_criteria>
    Dragging a pinned cursor no longer leaves selected text artifacts in the page, while pin reposition/remove outcomes remain unchanged.
  </acceptance_criteria>
  <done>Pin drag start/move/end paths suppress browser text selection and always restore normal selection behavior after gesture cleanup.</done>
</task>

<task type="auto">
  <name>Task 2: Scope CSS selection lock to active pin-drag state</name>
  <files>frontend/charts/styles/components.css, frontend/charts/components/d3-line-chart.js</files>
  <read_first>
    - frontend/charts/styles/components.css
    - frontend/charts/components/d3-line-chart.js
  </read_first>
  <action>
    Add a narrowly scoped CSS class used only during active pinned-cursor drag (for example on `body`) to suppress user text selection and keep drag affordance coherent.
    - Define class styling with `user-select: none` and webkit fallback while active.
    - Do not alter default non-drag selection behavior outside pin-drag lifecycle.
    - Keep existing cursor affordance (`ew-resize`) behavior intact per CHT-80.
  </action>
  <verify>
    <automated>node --check frontend/charts/components/d3-line-chart.js</automated>
    <automated>rg -n "user-select|pin-drag|cursor" frontend/charts/styles/components.css frontend/charts/components/d3-line-chart.js</automated>
  </verify>
  <acceptance_criteria>
    Selection suppression is active only while pinned drag is active, and normal selection resumes after drag end/delete/cancel.
  </acceptance_criteria>
  <done>Styles and runtime class toggling are scoped to active pin drag only, with no permanent selection lock side effects.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser pointer events -> chart interaction handlers | Untrusted, high-frequency input can trigger unintended UI/browser side effects. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05-01 | D | `d3-line-chart.js` pointer handlers | mitigate | Add deterministic cleanup for selection-lock state on every drag termination path to avoid stuck UI state. |
| T-08-05-02 | T | DOM selection state during drag | mitigate | Suppress default selection behavior only in active pin-drag mode; clear ranges during drag lifecycle. |
</threat_model>

<verification>
- Automated syntax and grep checks from both tasks must pass.
- Manual UAT replay (Test 5): create pin, drag off left/right edge, confirm pin deletion and no text highlight remains.
</verification>

<success_criteria>
- UAT Test 5 transitions from `issue` to `pass` with no new regressions in Tests 2, 4, and 7.
- Off-edge delete remains immediate and still frees a pin slot.
- Dragging pinned cursor no longer produces unwanted text selection artifacts.
</success_criteria>

<output>
After completion, create `.planning/phases/08-cross-chart-hover-and-pinned-cursors/08-05-SUMMARY.md`.
</output>
