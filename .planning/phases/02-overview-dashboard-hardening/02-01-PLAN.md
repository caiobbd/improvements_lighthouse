---
phase: 02-overview-dashboard-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
  - js/mock-data.js
autonomous: true
requirements: [OVW-02]
must_haves:
  truths:
    - "Daily accumulated KPI charts (Oil, Gas, Water, Flare) use correct labels and units."
    - "KPI chart metadata is centralized to reduce drift between titles, units, and datasets."
  artifacts:
    - path: "index.html"
      provides: "Overview chart metadata and rendering logic hardening"
      min_lines: 200
      contains: "Daily Accumulated"
    - path: "js/mock-data.js"
      provides: "Coherent 7-day accumulated datasets per KPI"
      min_lines: 120
      contains: "MOCK.oil"
  key_links:
    - from: "index.html"
      to: "js/mock-data.js"
      via: "render logic reads normalized KPI datasets"
      pattern: "MOCK"
---

<objective>
Harden 7-day accumulated KPI chart contracts and ensure unit/label correctness.
</objective>

<execution_context>
@C:/Users/CaioBorges/_pgms/_repos/improvements_lighthouse/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/CaioBorges/_pgms/_repos/improvements_lighthouse/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-overview-dashboard-hardening/02-CONTEXT.md
@.planning/phases/02-overview-dashboard-hardening/02-RESEARCH.md
@index.html
@js/mock-data.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Normalize KPI metadata and chart-label contracts</name>
  <files>index.html</files>
  <action>Refactor inline Overview chart setup so each accumulated KPI references a normalized metadata object with title, unit, and dataset keys instead of hardcoded scattered strings.</action>
  <verify>findstr /C:"Daily Accumulated" index.html</verify>
  <done>Accumulated KPI chart contracts are centralized and consistent.</done>
</task>

<task type="auto">
  <name>Task 2: Align mock accumulated datasets with chart expectations</name>
  <files>js/mock-data.js</files>
  <action>Validate and adjust Oil/Gas/Water/Flare 7-day accumulated arrays for coherent ranges and matching dataset lengths.</action>
  <verify>findstr /C:"MOCK.flare" js\\mock-data.js</verify>
  <done>Mock accumulated datasets are coherent and aligned with chart contracts.</done>
</task>

<task type="auto">
  <name>Task 3: Validate accumulated chart rendering paths</name>
  <files>index.html</files>
  <action>Confirm all accumulated chart constructors consume normalized metadata and retain legend/stack settings as intended.</action>
  <verify>findstr /C:"makeChart('chart-oil-bar'" index.html</verify>
  <done>All accumulated charts render through hardened metadata paths.</done>
</task>

</tasks>

<verification>
- [ ] Oil, Gas, Water, and Flare accumulated charts show expected units and labels
- [ ] No missing/undefined dataset errors in browser console
</verification>

<output>
After completion, create `.planning/phases/02-overview-dashboard-hardening/02-01-SUMMARY.md`
</output>
